info:
  name: View Data
  type: http
  seq: 2

http:
  method: GET
  url: "{{baseUrl}}/samehadaku/view-data"
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
